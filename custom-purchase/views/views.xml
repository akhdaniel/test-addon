<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="custom-purchase.list">
      <field name="name">custom-purchase list</field>
      <field name="model">custom-purchase.custom-purchase</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="custom-purchase.action_window">
      <field name="name">custom-purchase window</field>
      <field name="res_model">custom-purchase.custom-purchase</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="custom-purchase.action_server">
      <field name="name">custom-purchase server</field>
      <field name="model_id" ref="model_custom-purchase_custom-purchase"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="custom-purchase" id="custom-purchase.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="custom-purchase.menu_1" parent="custom-purchase.menu_root"/>
    <menuitem name="Menu 2" id="custom-purchase.menu_2" parent="custom-purchase.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="custom-purchase.menu_1_list" parent="custom-purchase.menu_1"
              action="custom-purchase.action_window"/>
    <menuitem name="Server to list" id="custom-purchase" parent="custom-purchase.menu_2"
              action="custom-purchase.action_server"/>
    -->
  </data>
</odoo>